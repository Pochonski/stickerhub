export interface PackCard {
  id: string;
  name: string;
  num?: number;
  pos?: string;
  gradient: string;
  teamColor: string;
  teamColorDark: string;
  isNew: boolean;
  teamId: string;
  faceUrl?: string;
}
